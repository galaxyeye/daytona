# daytona Docker image build configuration file
# You can copy this file as .env and modify the configuration

# Image registry configuration
REGISTRY=docker.io
NAMESPACE=galaxyeye88

# Image version
VERSION=latest

# Build platforms (comma-separated for multi-platform)
PLATFORM=linux/amd64,linux/arm64

# Services to build (comma-separated)
# Options: api, proxy, runner, docs
SERVICES=api,proxy,runner,docs

# Whether to push to registry (true/false)
PUSH=false

# Whether to disable build cache (true/false)
NO_BUILD_CACHE=false

# Whether to show verbose logs (true/false)
VERBOSE=false

# Build arguments (optional)
# Example build arguments:
# PUBLIC_WEB_URL=https://daytona.io
# PUBLIC_ALGOLIA_APP_ID=your_app_id
# PUBLIC_ALGOLIA_API_KEY=your_api_key

# Preset configuration examples:

# Development environment
# VERSION=dev
# PLATFORM=linux/amd64
# SERVICES=api,proxy,runner
# PUSH=false

# Production environment
# VERSION=v1.0.0
# PLATFORM=linux/amd64,linux/arm64
# SERVICES=api,proxy,runner,docs
# PUSH=true

# GitHub Container Registry
# REGISTRY=ghcr.io
# NAMESPACE=your-org
# PUSH=true
